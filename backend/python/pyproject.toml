[project]
name = "ultimate-portfolio"
version = "0.1.0"
description = "Portfolio optimization service."
requires-python = ">=3.11, <3.12"
dependencies = ["yfinance", "pandas", "numpy", "PyPortfolioOpt", "cvxpy", "fastapi", "uvicorn[standard]"]
